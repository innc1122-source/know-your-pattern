# PATTERNA — 产品与开发文档

> **PATTERNA**
> Notice what repeats. See what changes.
> 看见重复，也看见改变。
>
> 版本 v1.0 · 文档对应已交付的 `patterna.html`
> 本文档描述的是**已经实现并通过测试的东西**，不是计划。计划单列在第 11 节。

---

## 1. 这个产品是什么

一个记录工具。事情发生、让你在意的时候打开，一分钟内记完，然后它帮你看见什么在重复、以及你对它的反应有没有在变。

**它解决的问题：**

> 为什么类似的事情，总是让我有类似的反应？

以及更长期的那个：

> 我真的有在改变吗？

**它不解决的问题：** 今天心情怎么样、帮我写日记、给我建议、告诉我该怎么办。

### Slogan

**看见重复，也看见改变。**
*Notice what repeats. See what changes.*

只有一句，品牌和产品共用。

前半句是产品每天在做的事，后半句是它长期才兑现的东西——**第 7 天和第 90 天的价值都在这一句里**。拆开只留观察那一半，等于把长期价值扔了。

前半句也守住了原则 1：说的是「看见重复」，不是「理解自己」——不承诺答案。

### 定位

不是 mood tracker，不是日记，不是 AI 治疗师，也不完全是 AI 教练。

它是一个**个人模式观察工具**。

---

## 2. 六条产品原则

这六条不是文案，是判断依据。后面每一个技术阈值都能追溯到其中一条。

**1 · 先好奇，再下结论**
不说「你就是一个控制欲很强的人」，说「这里似乎反复出现了一个与自主权有关的信号」。

**2 · 模式不是人格**
描述行为与情境，不定义「你是谁」。

**3 · 用户是共同调查者**
不是被 AI 分析的对象。所有洞察都要给证据，用户可以否定，否定会改变系统的判断。

**4 · 先有证据，再有解读**
证据不够的时候不解读。第一次接触尤其如此。

**5 · 不强迫成长**
不打卡、不连续天数、不催促、不要求改变。这是一个 **event-driven** 产品，不是 daily habit app。

**6 · 先猜测，再建议**
优先说「我们猜下次可能会发生什么」，而不是「下次你应该怎么做」。
猜测制造好奇，建议制造负担。

### North Star

> **不要告诉用户「你是谁」。帮他们看见「自己正在成为谁」。**

---

## 3. 核心闭环

```
Moment      发生了一件值得记录的事
   ↓
Capture     一句话 + 四次点选（目标 60 秒内）
   ↓
Mirror      系统把用户自己的回答缝成一句话，不解读
   ↓
Connection  同一信号第 2 次出现时，跨记录连接
   ↓
Glimpse     信号开始累积，给出观察 + 请用户自己解释
   ↓
Prediction  系统承担风险，猜下次会怎样
   ↓
Validation  下次同信号出现时，问准不准
   ↓
Pattern     证据足够后成形，用户校准
   ↓
Change      长期对比：那时 → 现在
```

### 渐进解锁

这张表是留存设计的核心。**用户永远不需要「等到 90 天」，下一层价值始终就在前面。**

| 时点 | 得到什么 | 用户的感受 |
|---|---|---|
| 第 1 条 | Mirror | 它让我停下来看了一眼自己 |
| 第 2 条 | Connection | 它记得我之前发生过什么 |
| 第 3 条起 | Glimpse + 请你解释 | 好像有个东西正在出现 |
| Glimpse 之后 | Prediction | 它居然敢猜下一次 |
| 第 7 天 | 周回顾 | 帮我整理了这一周 |
| 同信号 3 次 | Changes 半张对比 | 我看到了「现在的我」 |
| 同信号 5 次 / 跨 3 周 | Pattern | 它开始真的认识我 |
| 同信号 6 次 / 跨 21 天 | Then → Now | 我看见自己在变 |

---

## 4. 词表

**词表是封闭的。这是整个产品最重要的技术决定。**

原因：跨时间比较要求同一件事每次得到同一个标签。如果标签会漂移——这次记成「自主权」下次记成「掌控感」——Then → Now 就算不出来，而那是产品最值钱的一页。

### 4.1 信号（10 个）

用户看到的是短句（`s`），不是分类名（`n`）。分类名只在模式卡等少数地方出现。**给人贴标签违反原则 2。**

| key | 用户看到的 | 分类名 |
|---|---|---|
| `autonomy` | 我没有选择 | 自主权 |
| `heard` | 没人听我说 | 被听见 |
| `fairness` | 不公平 | 公平 |
| `standards` | 达不到我的标准 | 标准 |
| `valued` | 没人注意到 | 被看见 |
| `certainty` | 不知道会怎样 | 确定性 |
| `capacity` | 事情太多 | 负荷 |
| `friction` | 我不想说 | 摩擦 |
| `belonging` | 我被排除在外 | 归属 |
| `competence` | 我被质疑了 | 胜任 |

每个信号带 9 个字段，中英各一套：

| 字段 | 用途 |
|---|---|
| `s` | 选项列表上的短句 |
| `n` | 分类名 |
| `g` | Glimpse 和 Connection 的观察句 |
| `p` | Prediction 的猜测句 |
| `t` | 完整的模式句（保留，当前未直接渲染） |
| `t1` / `t2` | 模式卡的对照两半：不是 t1，而是 t2 |
| `a` | 竞争性解释 |
| `q` | 周回顾里带向下一周的问题 |

### 4.2 其他词表

- **第一反应** 7 项（含「都不是」），带表情
- **强度** 3 项，`light`/`medium`/`strong` → 数值 1/2/3
- **你做了什么** 7 项（含「都不是」）

### 4.3 逃生门

四个列表都有「都不是」，**而且选了之后必须用自己的话说出来才能继续**。一个不接住后续的逃生门比没有更糟。

自定义的词会出现在 Mirror 上（「『被当成透明人』我们记下了」），但**永远不进引擎**：不计入排名、不算占比、不会长成模式。否则一堆「说不清」会互相聚成一个假信号。

### 4.4 词表会不会长

**会，但不能靠用户自由命名。**

如果开放自定义标签，同一件事这个月写「没被尊重」、下个月写「不被当回事」、再下个月写「被轻视」——在数据里这是三个东西，永远聚不到一起，Then → Now 直接失效。**封闭词表不是限制表达，是保护比较。**

所以词表从「都不是」的内容里生长：那些词全部留着、可查、可导出，**扩不扩、扩什么，由真实数据决定，不由设计者猜。**

判断标准是信号那一步的「都不是」点选率：

| 点选率 | 判断 | 界面上显示 |
|---|---|---|
| < 10% | 词表基本够用 | 词表基本接得住你遇到的事 |
| 10–30% | 缺一两类，看内容就知道缺什么 | 有几类东西反复落在词表之外 |
| > 30% | 词表方向错了，不是补几个的问题 | 词表缺了它本该有的东西 |

当前这十个信号明确没覆盖的：**正面情绪**（现在十个全是被戳到的）、**亲密关系里的距离与依赖**、**身体与状态**（累、饿、没睡好，很多情绪的真实触发在这里）、**具体文化处境**（跨语言团队里的层级预期与面子，任何通用词表都不会有）。

这些是否要补，等三十天的真实点选率出来再决定。

---

## 5. 引擎

纯函数，无 DOM 依赖，无网络，无模型。文件：`kyp-engine.js`。

### 5.1 为什么规则就够

语义在用户点选的那一刻就产生了——**最难的一步是人做的，不是程序做的**。剩下的全是计数、日期分布和前后半段比较。

由此得到三个好处：

- **可解释**：证据页能列出具体是哪几条记录，因为判断本来就是从它们数出来的
- **稳定**：同样的数据永远得出同样的结论
- **数据不出设备**：「记录只留在这台设备上」是架构决定的，不是承诺

### 5.2 阈值

全部集中在 `KYP.TH`，是唯一需要用真实数据调的东西。

```js
connectMin:     2     // 同一信号第 2 次 → Connection
glimpseCount:   3     // Glimpse 需要的次数
glimpseDays:    2     // 且跨至少这么多个不同的日子
glimpseWindow:  8     // 只看最近 N 条
patternCount:   5     // Pattern 需要的次数
patternWeeks:   3     // 且跨至少这么多周
patternShare:   0.30  // 且占全部信号的比例不低于此
patternLead:    1.5   // 且领先第二名这么多倍
changeMin:      6     // 同一信号累积到这么多次才比较
changeSpanDays: 21    // 且时间跨度不少于这么多天
```

### 5.3 每条阈值的理由

**`glimpseDays: 2`** — 三条记录都在同一天不触发 Glimpse。
**一个糟糕的日子不是一个模式。** 不拦的话，用户在最崩溃那天连记三条就会收到一张模式卡——那是最伤人的时刻说最武断的话。

**`patternLead: 1.5`** — 5 次自主权 vs 4 次公平不算模式。
那不是一个模式，是两件事。只看占比拦不住这种情况。

**`changeMin: 6` + `changeSpanDays: 21`** — 两个条件都要满足。
少于 6 次算出来的百分比毫无意义（3 比 3，一条就能翻转）；少于 21 天看到的是心情起伏，不是变化。
**这一页说错一次，之后它说什么都没人信了。**

**`patternCount: 5` + `patternWeeks: 3`** — 五次挤在一周内不算，必须跨周。

### 5.4 可信度

1–5 分，会因为用户否定而**下降**。

```
起始 1 分
+1  次数达到 patternCount
+1  跨越 ≥4 周
+1  占比 ≥45%
+1  用户确认 ≥2 次
-1  用户否定 ≥2 次
```

**可信度会掉，是原则 3 的技术体现。**

### 5.5 API

| 函数 | 返回 |
|---|---|
| `counts(ms)` / `ranked(ms)` | 信号频次 / 排序（自动排除 `other`） |
| `connection(ms, m)` | 该信号之前是否出现过 |
| `glimpse(ms)` | 正在浮现的信号；已成 Pattern 时返回 null |
| `pattern(ms)` | 模式 + 可信度 + 竞争解释 |
| `change(ms, sig)` | 反应分布对比 + 强度对比 |
| `changesAll(ms)` | 所有能比较的信号 |
| `snapshotNow(ms, sig)` | 「现在的样子」，3 条即可（因为不声称趋势） |
| `unlockProgress(ms)` | 距离第一次对比还差多少次、多少天 |
| `weeklySnapshot(ms)` | 周回顾；不满一周或本周少于 2 条返回 null |
| `makePrediction(ms, sig)` | 生成一条待验证的猜测 |
| `checkablePrediction(preds, m)` | 新记录能否验证某条待验证的猜测 |
| `predictionScore(preds)` | 已验证猜测的命中率 |
| `offListReport(ms)` | 「都不是」的全部内容、各字段点选率、词表判定 |
| `weekOf(ms, ts)` / `dayKey` / `weekKey` | 时间分桶辅助函数 |

**测试：54 项，全过。** 文件 `kyp-engine.test.js`，`node kyp-engine.test.js` 即可跑。

---

## 6. 选项排序

**措辞永远不变，只变顺序。**

写完那句话之后，按关键词给 10 个信号打分（共 140 个关键词，中英混合），把最可能的 3 条顶上来，其余收在「其他可能 · 7」后面。

为什么不用模型重写描述：**如果十条都被改写得贴合他的情境，十条都会显得像。** 现在这些描述有用，恰恰因为它们互相有对比——只有一条戳中，其余九条明显不是。

猜错零代价：完整列表一点就开，顺序也还在。而且**经常需要点开这件事本身，就是关键词表该补的信号**。

---

## 7. 数据

### 7.1 一条记录

```js
{
  id, ts,                    // 唯一 id、时间戳
  text,                      // 用户写的那一句
  reaction,                  // 第一反应
  signal,                    // 信号 key，或 'other'
  intensity, iv,             // 强度 key 与数值 1/2/3
  response,                  // 你做了什么
  note,                      // 用户自己的解释（可空）
  confirm,                   // 模式校准：yes / maybe / no
  other: { [field]: '...' }, // 各字段的自定义输入
  secs                       // 完成耗时
}
```

### 7.2 全量结构

```js
{
  moments: [],
  predictions: [{ id, signal, createdTs, status, result, closedTs }],
  seen: { glimpse:{}, pattern:{}, week:'' },   // 防止重复弹同一张卡
  lang, onboarded, created
}
```

### 7.3 存储

- `localStorage`，键 `kyp.v1`
- 不可用时自动降级为内存模式，并在顶部提示
- 可完整导出 / 导入 JSON

**数据跟着「网址 + 浏览器」走。** 换域名、换浏览器、换设备都不通用，必须靠导出导入迁移。

**已知风险：iOS Safari 有七天未访问即清除的机制。** 而这是个「有事才用」的产品，两周不打开很正常。**定期导出不是可选项。**

---

## 8. 界面

四个 Tab：

| Tab | 回答的问题 |
|---|---|
| 首页 | 现在要不要记一条 |
| 模式 | 我身上什么在重复 |
| 变化 | 我有没有在改变 |
| 我 | 语言、数据、重看开场、词表回顾 |

「我」下面还有一个二级页面 **词表没接住的东西**——不是 Tab，因为它是工作台不是日常界面。里面按次数排列所有「都不是」的内容，标明来自哪一个问题、出现过几次、最近一次是什么时候，顶部给出词表判定。导出的 JSON 里它单独成段（`_offList`）。

### 8.1 记录流程（5 步）

一句话 → 第一反应 → 哪一点最戳你 → 这次有多强 → 然后你做了什么

**为什么是强度而不是「你在保护什么」：** 后者和上一步问的是同一件事（「我没有选择」vs「我的自主权」是同义反复），而且引擎用不到。强度给出的是上一步推不出来的信息，并且让 Changes 能显示第二种变化。

### 8.2 Day 1 Mirror 规则

**第一条记录时，系统对这个人一无所知，所以不允许解读。**

只做一件事：把用户自己的回答缝成一句话。

> 你选择了
> **「我没有选择」**
> 以及
> **「当时挺明显的」**
>
> 「自主权」我们记下了。如果它再出现，我们会告诉你。

引号和主色只用在用户自己的词上——**在这一屏，紫色的含义是「这是你说的，不是我的判断」**。

最后那句前瞻承诺是零解读风险的价值兑现：不解释任何东西，但给了一个具体的、可兑现的期待。

### 8.3 Pattern 卡

拆成对照两半，因为**转折本身就是洞察**：

> 你可能不是最在意
> 工作变多（灰）
> 而是更在意
> **这件事没有让你参与决定**（主色）

必须同时出现：可信度、依据的记录、竞争性解释、三个校准按钮。

**三个校准按钮同色。** 把「不太对」染成红色等于把用户不同意渲染成错误——但那是校准，是这个产品最想要的信号。

### 8.4 Changes 的三个阶段

1. **完全没数据** → 带「示例」标签的示意 + 说明需要什么
2. **同信号 ≥3 次** → 半张对比：「那时 — —（历史还不够）→ 现在 67% 当场就反应了」，并明确说还差几次、还差几天
3. **达标** → Then → Now，可能显示两种变化：
   - 反应变了：「触发你的事情并没有消失，**变化的是你**」
   - 强度降了：「这件事发生的频率没有变，**它现在没那么疼了**」

3 次就敢显示右半边，是因为**它没有声称任何趋势**，只是「目前为止的样子」。同一个百分比，当成对比 3 条不够，当成现状 3 条就够。

### 8.5 周回顾

满一周后出现在首页顶部：本周条数、出现最多的信号、最常见的第一反应、用户自己写过的一句话、以及一个带向下周的问题。

**本周少于 2 条完全不出现。** 不催、不提「完成度」——安静本身就是在兑现原则 5。

---

## 9. 视觉

### Logo

字母 P 内嵌一条莫比乌斯环——**它自己就是「重复」的形状**，回到起点但已经翻了一面，和产品讲的是同一件事。

内嵌为 base64，无外部依赖。出现在开场页和「我」页面，另作 favicon、主屏图标与 PWA manifest 图标。

**方向：暗房。** 产品做的事就是让一个本来看不见的东西慢慢显影。

- **默认深色**（潜影未显），浅色跟随系统（洗出来的样子）
- **主色紫**，从深紫到亮紫的渐变；**信号出现得越多，那根条越亮**——重复被渲染成显影
- **琥珀只留一处**：Glimpse 那句话的荧光涂痕。全屏唯一的暖色，产品最重要的那句话被单独拎出来
- **产品说话的卡片是青绿色**，和交互色完全分开，永远不会被误认成按钮
- 字体：Plus Jakarta Sans + 思源黑体（界面）／Newsreader + 思源宋体（**只在产品开口时使用**）
- 深浅两套全部文字对比度过 WCAG AA
- `prefers-reduced-motion` 全部动效可关

---

## 10. 双语

中英完整两套，共 133 个 copy key，运行时切换。

**说话人标签在中文里是角色，在英文里是品牌名。** 中文版是「我们注意到」，英文版是 PATTERNA。直译成「了解你的模式」会变成一句宣言，而那个位置需要的是一个说话人。

测试里有一道扫描：中文版四个 Tab 加完整记录流程，任何漏掉的英文都会让测试失败。放行两个：品牌名 PATTERNA（不翻译），以及「我」页面里那个 English 按钮——切换语言的按钮本来就该用它要切过去的语言写。

---

## 11. 待办

### 应该尽快做

| 项 | 理由 |
|---|---|
| **认真保存校准数据** | 现在用户否定只写了一个 `confirm:'no'`，没记是哪张卡、当时可信度多少、基于哪些证据，也没问「哪里不对」。**这是唯一在积累的资产，现在每否定一次就丢一次信息。**（词表这一侧已经做完了，见 4.4；模式校准这一侧还没有。） |
| **记录可编辑 / 可删除** | 现在选错了没法回头。人对自己的记录会反悔。 |

| **导出提醒** | 超过 14 天没导出就在「我」页面提示。配合 iOS 七天清除机制，这是防丢的最后一道。 |
| **未验证的 Prediction 出口** | 那个信号两周没出现，猜测就永远悬着。需要能看到「还开着的猜测」并主动认领。 |

### 明确暂缓

| 项 | 为什么不做 |
|---|---|
| **模型预填标签** | 能把 40 秒压到 10 秒，但会让「数据不出设备」作废——那是现在最干净的立场。等验证过人们真的愿意持续记录之后再说。如果没人记，快 30 秒也没用。 |
| **记录后的 AI spark** | 本地已有 10 句教练问题（周回顾在用）。先把它接到记录流程后面看有没有用；如果本地版就够，省下一整套复杂度。 |
| **日历/照片自动提醒** | 会让产品从 event-driven 变成 push-driven，直接违反原则 5。 |
| **社群、对比、排行** | 任何社交元素都会让人开始表演，记录立刻失真。 |
| **年度报告** | 很值钱，但现在一个用户都没满一年。 |

---

## 12. 商业

### 付费墙

**发现自己免费，长期理解自己收费。**

- **免费永久**：记录、Mirror、Connection、Glimpse、Prediction、周回顾、第一个 Pattern、基础 Changes
- **Pro**：完整模式地图、全部证据历史、模式演化、Then → Now 全历史、月度/季度回顾、年度报告

划在这里的好处是它和产品自身逻辑重合——模式本来就需要积累才出现，付费墙不是被塞进来的，是自然长出来的。

### 定价

不拍板，Beta 阶段测 **$59 / $89 / $129 每年** 三档。

首要验证的是「有没有人愿意为纵向洞察付费」，不是「哪个价格收入最大」。

参考行业数据：freemium 产品 D35 中位付费转化约 2.1%。按 $129/年、2% 转化算，做到 $2,000/月大约需要 190 个付费用户、约 9,500 个注册用户。

### 护城河

**诚实说：现在没有，有的是一段领先时间。**

- 引擎、产品设计、离线立场——都能被复制，两周的事
- **用户纵向数据**——真的切换成本，但只保护老用户，冷启动时等于零
- **词表与阈值的调校**——需要几百个真实用户才知道，最接近护城河，但现在还没有
- **判断标准本身（什么时候有资格下结论）**——不在界面上、需要专业判断加真实数据、随每次用户校准变强

真正的问题是：**在别人复制完之前，能不能积累到足够的校准数据。**

还有一个更冷的可能：如果它最终稳定在几千付费用户、一年几万美元，那它对任何有资源的公司都不值得抄。**小本身就是一种保护。**

---

## 13. 工程

### 文件

```
patterna.html             单文件成品，直接打开即用
kyp-engine.js             引擎，纯函数，可独立引用/移植
kyp-engine.test.js        引擎测试，54 项
```

源码分四块，构建时内联进 `shell.html`：

```
engine.js   引擎
app.js      词表 + 双语 copy + 存储层
ui.js       界面与流程
shell.html  外壳与样式
```

### 测试

- **引擎 54 项**：阈值边界、可信度升降、强度对比、预测计分
- **应用 107 项**：jsdom 真跑，覆盖开场、四个 Tab、完整记录流程、每一种揭示、逃生门、排序、双语无英文残留、持久化与导入导出

**为什么必须跑真实 DOM：** 有个 bug 语法检查完全发现不了——`<div id="flow">` 会自动创建同名全局变量，把状态变量遮住，一点就崩。这类问题只有真跑才暴露。

### PWA

manifest 内嵌为 data URI，`display: standalone`，可「添加到主屏幕」后全屏运行、带图标。

### 部署

单文件静态托管即可。GitHub Pages 路径：仓库传 `index.html` → Settings → Pages → 选 `main` 分支。

**页面是公开的，内容是私有的**——数据在浏览器本地，从不上传。

### 移植到后端

`kyp-engine.js` 无任何环境依赖。要迁到 FastAPI + SQLite 时，直接翻成 Python，45 个测试一起翻，行为不会变。

---

## 14. 还没被验证的事

**下面这些没有一条有答案，而它们比任何功能清单都重要。**

1. 难受的当下，人会不会真的打开它？**如果不会，后面全部不成立。**
2. 十个信号够不够用？「都不是」的点选率有多高？**这一条现在可以直接测了**——「我 → 词表没接住的东西」会给出答案。
3. Glimpse 第一次跳出来的时候，用户信不信？
4. 有多少人会写那句「你觉得这是怎么回事」？
5. 三个月后，Changes 那一页是不是真的给了别处得不到的东西？

**下一步不是继续写代码，是自己用满 30 天。**
