把这个文件夹里的 index.html 上传到任何静态托管，就能用。

GitHub Pages:
  1. 新建 public 仓库
  2. Add file → Upload files，上传 index.html
  3. Settings → Pages → Source 选 main 分支 → Save
  4. 等一两分钟，访问 https://<用户名>.github.io/<仓库名>/

手机打开后可「添加到主屏幕」，全屏运行、带图标、离线可用。

页面是公开的，内容是私有的 —— 数据存在访问者自己的浏览器里，从不上传。
